package com.higgschain.trust.slave._interface.pack;

import com.higgschain.trust.slave._interface.InterfaceCommonTest;

/**
 * The type Pack submit test.
 */
public class PackSubmitTest extends InterfaceCommonTest{
    private static String PROVIDER_ROOT_PATH = "java/com/higgs/trust/slave/core/service/pack/submit/";

    @Override protected String getProviderRootPath() {
        return PROVIDER_ROOT_PATH;
    }
}
