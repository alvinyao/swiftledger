language: java

jdk:
  - oraclejdk8

cache:
  directories:
    - .autoconf
    - $HOME/.m2
addons:
  sonarcloud:
    organization: "aurorasic-github"

after_success:
  - mvn -s ./bin/setting.xml deploy -DskipTests=true -Dmaven.javadoc.skip=true
  - mvn jacoco:report  sonar:sonar
  
